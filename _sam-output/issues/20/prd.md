# PRD: Change color scheme to wooden brown

> **Issue:** #20
> **Repository:** sam-ai-bot/test2
> **Generated:** 2026-01-05T01:26:31.618Z
> **Type:** feature
> **Priority:** medium
> **Labels:** feature, enhancement, UI

---

## Overview

Change color scheme to wooden brown

## Original Request

Change color scheme to wooden brown

---

Now I have a complete understanding of the codebase. Let me create the implementation plan:

---

## 🎯 Implementation Plan

### Summary
Update the Chess Academy website's color scheme from the current "Chess Green" theme to a "Wooden Brown" theme by modifying CSS custom properties in the centralized `styles.css` file. This is a straightforward theme change that leverages the well-architected CSS variable system.

### Tasks

- [ ] **Task 1: Define the Wooden Brown Color Palette**
  - Research/define appropriate wooden brown hex values for each variable:
    - `--color-black`: Deep mahogany (e.g., `#4E342E`)
    - `--color-dark`: Rich walnut brown (e.g., `#5D4037`)
    - `--color-white`: Light maple/cream (e.g., `#FDF6E8`)
    - `--color-off-white`: Very light wood (e.g., `#F5EFE0`)
    - `--color-accent`: Peru/honey wood (e.g., `#CD853F`)
    - `--color-accent-dark`: Sienna (e.g., `#A0522D`)
    - `--color-gray`: Warm muted brown (e.g., `#8D6E63`)
    - `--color-light-gray`: Light warm tone (e.g., `#D7CCC8`)
  - Ensure sufficient contrast ratios for accessibility (WCAG 2.1 AA)
  - Acceptance: Color palette documented with hex values and passes contrast checks

- [ ] **Task 2: Update CSS Color Variables**
  - Modify `css/styles.css` lines 4-11 (`:root` selector)
  - Replace all 8 color variable values with the wooden brown palette
  - Acceptance: All `--color-*` variables updated to brown tones

- [ ] **Task 3: Update Shadow Colors**
  - Modify `css/styles.css` lines 15-18
  - Change shadow `rgba()` values from green-tinted (`rgba(27, 77, 62, ...)`) to brown-tinted (e.g., `rgba(78, 52, 46, ...)`)
  - Update `--shadow-light`, `--shadow-medium`, and `--shadow-heavy`
  - Acceptance: All shadow variables use warm brown tint

- [ ] **Task 4: Visual QA Testing**
  - Open `index.html` in browser
  - Verify header/navigation styling
  - Verify hero section gradient
  - Verify course cards and buttons
  - Verify about and contact sections
  - Verify footer styling
  - Check all hover states and transitions
  - Acceptance: All sections render correctly with new wooden brown theme

- [ ] **Task 5: Accessibility Verification**
  - Test text contrast on all backgrounds
  - Verify button states are distinguishable
  - Check focus indicators remain visible
  - Acceptance: Meets WCAG 2.1 AA contrast requirements

### Technical Considerations
- **Single file change**: All color modifications are centralized in `css/styles.css` (lines 3-19)
- **No build process required**: This is a vanilla HTML/CSS project - changes take effect immediately
- **CSS Variables**: The app uses CSS custom properties, so updating the `:root` variables cascades to all 60+ color usages automatically
- **Previous palette available**: Git history contains a prior wooden chess theme (commit `f3fb384`) that can serve as reference

### Dependencies
- None - this is a self-contained CSS change with no external dependencies
- Reference: Prior wooden theme available in git history if needed

### Estimated Complexity
**Simple** - This is a straightforward color palette swap. The codebase is well-architected with all colors defined as CSS variables in one location. The change involves updating ~12 lines of CSS with no logic changes required.

---
*🏷️ Add label `ready-for-dev` when ready for implementation*


---

## Notes

- Target Directory: `/tmp/sam-repos/sam-ai-bot/test2`
- Tech Stack: JavaScript

---

*🤖 Generated by Sam 🎯 (Triage) → Pete 📋 (Planning) → Alex 🏗️ (Architecture)*
